"""Domain models and business rules.

Populated by the database-schema and business-configuration milestones.
Everything tenant-related that api, voice, and worker must agree on
lives here — never duplicated per service.
"""
