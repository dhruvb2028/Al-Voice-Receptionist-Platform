# Evaluation harness

Automated conversation evaluations arrive with the evaluation-harness
milestone. Scenarios (booking happy path, emergency escalation,
hallucination probes, prompt-injection resistance) run in CI and on a
nightly schedule; safety-scenario failures block deployment.
